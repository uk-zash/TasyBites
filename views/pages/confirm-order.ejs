<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> - New Tasty Bites</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/nav.css">
  <link rel="stylesheet" href="/css/menu.css">
</head>
<body>
  <%- include('../partials/_navbar') %>


  <div class="container mx-auto px-4 mt-20">
    <div class="container mx-auto mt-24 px-4 text-center">
      <h1 class="text-3xl font-semibold mb-6">Almost Done!</h1>
      <p>Click the button below to place your order via WhatsApp.</p>
      <div class="mt-6">
        <a href="<%= req.session.whatsappURL %>" target="_blank" class="w-full md:w-auto bg-green-500 text-white py-2 px-6 rounded hover:bg-green-600">
          Place Order on WhatsApp
        </a>
      </div>
    </div>
  </div>

  <%- include("../partials/_footer") %>

  <script>
    // Get elements
    const hamburgerMenuButton = document.getElementById("hamburger-menu");
    const mobileMenu = document.getElementById("mobile-menu");

    // Toggle menu visibility on hamburger click
    hamburgerMenuButton.addEventListener("click", function() {
      mobileMenu.classList.toggle("open"); // Show or hide the mobile menu
      hamburgerMenuButton.classList.toggle("active"); // Toggle hamburger to cross and vice versa
    });
  </script>
</body>
</html>
