<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= product.description %>">
    <title><%= product.name %>New Tasty Bites - Best Fast Food in Mahul, Azamgarh | Eat Pizza Burger, Drink Lassi Faluda , Enjoy</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/nav.css">
    <link rel="stylesheet" href="/css/menu.css">
    <link rel="stylesheet" href="/css/floating.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/product.css">
    <style>
  
    </style>
</head>
<body>
    <%- include('../partials/_navbar') %>

    <main class="product-container">
        <!-- Product Grid -->
        <div class="product-grid">
          <!-- Product Image -->
          <div class="product-image-container">
            <img src="<%= product.imageURL %>" alt="<%= product.name %> - New Tasty Bites, Mahul Azamgarh , Phoolpur" class="product-image" loading="lazy">
          </div>
      
          <!-- Product Info -->
          <div class="product-info">
            <h1 class="product-title"><%= product.name %></h1>
            <p class="product-price">₹<%= product.price %></p>
            <p class="product-description"><%= product.description %></p>
            
            <!-- Add to Cart Form -->
            <button data-id="<%= product.id %>" class="add-to-cart-button bg-blue-500 hover:bg-blue-600">
             <i class="fas fa-cart-plus"></i>  Add to Cart
            </button>
          </div>
        </div>
      
        <!-- Reviews Section -->
        <section class="reviews-section">
          <h2 class="reviews-heading">Customer Reviews</h2>
      
          <% if (reviews && reviews.length > 0) { %>
            <% reviews.forEach(function(review) { %>
              <div class="review-card">
                <!-- Reviewer Header with Dummy Profile -->
                <div class="review-header">
                  <div class="flex-shrink-0">
                    <div class="w-10 h-10 rounded-full bg-gray-200 flex items-center justify-center">
                      <i class="fas fa-user text-gray-500"></i>
                    </div>
                  </div>
                  <div>
                    <p class="review-author"><%= review.name %></p>
                    <p class="review-date">
                      <%= new Date(review.timestamp.toDate()).toLocaleDateString('en-IN', {
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric'
                      }) %>
                    </p>
                  </div>
                </div>
                <p class="review-text"><%= review.reviewText %></p>
              </div>
            <% }); %>
          <% } else { %>
            <div class="review-card">
              <p class="review-text">No reviews yet. Be the first to share your experience!</p>
            </div>
          <% } %>
      
          <!-- Review Form -->
          <div class="review-form">
            <h3 class="form-heading">Write a Review</h3>
            <form action="/product/<%= product.id %>/review" method="POST">
              <div class="form-group">
                <label class="form-label">Name</label>
                <input type="text" name="name" class="form-input" required>
              </div>
              
              <div class="form-group">
                <label class="form-label">Email</label>
                <input type="email" name="email" class="form-input" required>
              </div>
              
              <div class="form-group">
                <label class="form-label">Your Review</label>
                <textarea name="reviewText" rows="4" class="form-input" required></textarea>
              </div>
              
              <button type="submit" class="submit-btn">
                Submit Review
              </button>
            </form>
          </div>
        </section>
      </main>

    <%- include("../partials/_footer") %>
    <%- include("../partials/_floating_cart") %>

    
</body>
</html>